# travis config

language: r
sudo: false
dist: trusty

r_github_packages:
  - berndbischl/BBmisc
  - jimhester/covr
  - berndbischl/ParamHelpers

script: R CMD check .

after_success:
  - Rscript -e 'library(covr);coveralls()'

notifications:
  email:
    on_success: change
    on_failure: always
